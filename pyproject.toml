[project]
name = "fly-in"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
    "pre-commit>=4.6.0",
    "pydantic>=2.13.3",
    "questionary>=2.1.1",
    "raylib>=5.5.0.4",
    "ruff>=0.15.12",
]

[tool.ruff]
line-length = 79

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
